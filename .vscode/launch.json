{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ARES Command",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/venv_lin/bin/ares",
            "args": [
                "pipeline",
                "--workflow",
                "${workspaceFolder}/examples/workflow/workflow_example_1.json",
                "--output",
                "${workspaceFolder}/examples/output/"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
